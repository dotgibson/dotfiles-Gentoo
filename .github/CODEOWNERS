# CODEOWNERS — one author, so a single catch-all owner is deliberate.
#
# NB core/.github/CODEOWNERS exists too, but GitHub only reads CODEOWNERS from the
# repo root, .github/, or docs/ — a vendored copy nested under core/ is inert here.
# That is also true of the issue templates and the PR template next to this file:
# they are duplicated rather than reused because GitHub will not follow them into a
# subdirectory.
* @gerrrt

# The vendored Core subtree is not editable here at all — it is overwritten by the
# next `make sync` from dotfiles-core. core-integrity.yml enforces this at PR time;
# the owner line is a second signal on any PR that touches it.
/core/ @gerrrt
